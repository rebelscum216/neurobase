---
slug: release-v0.1.0
status: awaiting-review
author: claude
reviewer: codex
branch: release-v0.1.0
diff: git diff main...HEAD
created: 2026-07-14
---

# Review: v0.1.0 version bump (final pre-tag check)

## Brief  _(Author — Claude)_

**Intent.** This is the exact commit intended to become the `v0.1.0` git tag
and trigger the real PyPI publish via `.github/workflows/release.yml`
(already reviewed and merged in PR #1 / the Phase 9 release-prep relay). The
user asked for one more review pass specifically on this version-bump commit
before the tag/publish/release happens — that step is irreversible (a PyPI
version can't be deleted or reused), so this is the last checkpoint.

**Scope.** Branch `release-v0.1.0` off `main` (which already includes the
merged Phase 9 docs), `git diff main...HEAD`. Two commits:
- `release: bump to 0.1.0` — `pyproject.toml` (`0.1.0.dev0` → `0.1.0`,
  `Development Status :: 2 - Pre-Alpha` → `3 - Alpha`), `README.md` (status
  banner now describes a real 0.1.0 release instead of "pending"; Quickstart
  leads with `uv tool install neurobase-cli`, demotes the from-source path to
  secondary, removes the now-redundant "once published" blurb further down).
- `chore: sync uv.lock` — `uv.lock`'s self-referencing `neurobase-cli`
  package entry updated to match the new version (`uv build`/`uv sync`
  regenerated this automatically; not hand-edited).

**Focus areas.**
- Does the README's Quickstart reordering read correctly end-to-end — no
  dangling references to the old "not published yet" framing anywhere else
  in the file?
- Is claiming "the first public release" / describing `0.1.0` as shipped
  premature given the actual PyPI publish hasn't happened yet at the time
  this commit lands on `main` (there's a real window between this merge and
  the tag+release actually firing)? Author's view: acceptable, since this
  commit's entire purpose *is* to become the tagged, published state within
  minutes — but flag if this reads as dishonest in the interim.
- `Development Status :: 3 - Alpha` vs staying at `2 - Pre-Alpha` — is Alpha
  a fair classifier given the "live-verified by the maintainer, not
  battle-tested by strangers" framing the README itself uses two sentences
  later?

**Known risks / tradeoffs.**
- No `src/neurobase/` or test changes — version/metadata and docs only.
- The pre-existing uncommitted edit to
  `docs/notes/2026-07-09-phase-8-recommender-plan.md` in the working tree is
  unrelated prior work, not part of either commit on this branch — ignore it.
- This branch is not yet tagged or released; that happens only after this
  review approves and the user gives explicit go-ahead.

**How to verify.**
- `make ci` (green: 481 passed, ruff/format/mypy clean).
- `uv build` from this branch — confirms `neurobase_cli-0.1.0.tar.gz` /
  `-0.1.0-py3-none-any.whl` build cleanly with the real version string
  (author ran this; both artifacts built, then removed from the working tree
  since `dist/` is gitignored).
- Read the full README top-to-bottom for internal consistency now that the
  "not published" framing is gone.

**Out of scope.** Re-reviewing the Phase 9 docs content itself
(`SECURITY.md`, `CONTRIBUTING.md`, `docs/architecture.md`,
`docs/adapter-guide.md`, the comparison table, etc.) — that already went
through 4 rounds in `docs/reviews/2026-07-13-phase-9-release-prep.md` and
merged. This review is scoped to the version-bump diff only.

---

## Reviewer findings  _(Reviewer — Codex)_

> Run the diff and review the actual code. One entry per finding.

**Verdict:** _pending_
