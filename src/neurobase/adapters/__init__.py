"""Per-agent adapters: deterministic scribes (capture) and recall (inject).

Each adapter is isolated so an agent's contract shift has a one-module blast radius.
Claude in Phase 4, Codex in Phase 5.
"""
